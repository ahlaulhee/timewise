import { Inter, Poppins, Ubuntu, Work_Sans, Quicksand } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const poppins = Poppins({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
  variable: "--font-poppins",
});

export const ubuntu = Ubuntu({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
  variable: "--font-ubuntu",
});

export const worksans = Work_Sans({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
  variable: "--font-work-sans",
});

export const quicksand = Quicksand({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
  variable: "--font-quicksand",
});
